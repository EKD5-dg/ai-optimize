<Window x:Class="AiOptimize.Views.BigFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="大文件查找"
        Height="600" Width="780" MinHeight="420" MinWidth="620"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Microsoft YaHei UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="最占空间的大文件" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <ProgressBar Height="8" IsIndeterminate="True" Maximum="100"
                         Style="{StaticResource SlimProgressBar}" Foreground="{StaticResource AccentBrush}"
                         Visibility="{Binding IsScanning, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource SubTextBrush}"
                       FontSize="12" TextWrapping="Wrap" Margin="0,6,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Margin="0,4" Padding="14,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                        <Border CornerRadius="4" Padding="6,1" Margin="8,0,0,0"
                                                Background="#33FFFFFF" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding RiskLabel}" Foreground="{Binding RiskBrush}"
                                                       FontSize="11" FontWeight="SemiBold"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding KindDescription}" Foreground="{Binding RiskBrush}"
                                               FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding Directory}" Foreground="{StaticResource SubTextBrush}"
                                               FontSize="11" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding Bytes, Converter={StaticResource BytesToText}}"
                                           Foreground="{StaticResource AccentBrush}" FontWeight="Bold"
                                           VerticalAlignment="Center" Margin="12,0"/>
                                <Button Grid.Column="2" Content="打开位置" Width="82" Height="30"
                                        Style="{StaticResource AccentButton}" FontSize="12" FontWeight="Normal"
                                        Command="{Binding OpenLocationCommand}" Margin="0,0,8,0"/>
                                <Button Grid.Column="3" Content="删除" Width="64" Height="30"
                                        Style="{StaticResource AccentButton}" FontSize="12" FontWeight="Normal"
                                        Command="{Binding DeleteCommand}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
